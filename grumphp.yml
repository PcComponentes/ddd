grumphp:
    tasks:
        composer:
            strict: true
        phpstan:
            autoload_file: phpstan.neon
            configuration: ~
            level: 3
        jsonlint: ~
        phpcs:
            standard:
                - 'phpcs.xml.dist'
            whitelist_patterns:
                - '/^src\/(.*)/'
            encoding: 'UTF-8'
        phplint: ~
        phpunit: ~
