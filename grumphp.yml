grumphp:
    tasks:
        composer:
            strict: true
        phpcs:
            standard:
                - 'phpcs.xml.dist'
            whitelist_patterns:
                - '/^src\/(.*)/'
            encoding: 'UTF-8'
        phpunit: ~